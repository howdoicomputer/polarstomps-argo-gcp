apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: polarstomps-metrics
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: polarstomps
  endpoints:
  - port: polarstomps
    interval: 30s
